export interface CompanyData {
	address: string;
	description: string;
	email: string;
	founders: string;
	name: string;
	phone: string;
	// TODO: Add more fields for company data from the hubspot form
}
